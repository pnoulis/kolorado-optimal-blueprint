#!/bin/bash

./configure --dev --statedir='${BUILDIR}'
# ./configure --dev \
#           --buildir=build \
#           --distdir=dist \
#           --url=http://localhost \
#           --port=8080 \
#           --db-path=build/boptimus.db
